---
id: LRN-024
type: learning
date: 2026-06-11
tags: [dev-browser, screenshot, api, quickjs, playwright]
---

# LRN-024 — `saveScreenshot(buf, name)` : buffer en premier, filename en second

| Pattern observé                                                    | Contexte                                                                                                                                         | Application future                                                                          |
| ------------------------------------------------------------------ | ------------------------------------------------------------------------------------------------------------------------------------------------ | ------------------------------------------------------------------------------------------- |
| `saveScreenshot(buf, "file.png")` — Buffer d'abord, string ensuite | 3 échecs : `saveScreenshot("name")` → "File name must be empty string", `saveScreenshot()` → idem, `saveScreenshot(buf)` seul → "not a function" | `const buf = await page.screenshot(); const path = await saveScreenshot(buf, "debug.png");` |

## Références

- [ZBLK-014](../archive/blockers/ZBLK-014.md) — blocker associé, 3 tentatives
