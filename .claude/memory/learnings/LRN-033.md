---
id: LRN-033
type: learning
date: 2026-06-12
tags:
  [
    react,
    game-design,
    pool,
    random,
    useMemo,
    useState,
    flashcards,
    architecture,
  ]
---

# LRN-033 — Pool dynamique `buildRoundPool()` + `useMemo(roundMap)` : sélection variable par partie

| Pattern observé                                                                                                                                          | Contexte                                                                                                                                                                                                                 | Application future                                                                                                                       |
| -------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | ---------------------------------------------------------------------------------------------------------------------------------------- |
| Séparation factory `buildRoundPool()` + `useState(pool)` + `useMemo(roundMap)` permet un pool différent à chaque partie sans recréer la Map manuellement | `PhotoFlashcardTab` : 44 photos disponibles, 11 sélectionnées aléatoirement par partie (1 par personnage). `handleReplay` appelle `buildRoundPool()`, update le state, la Map est recalculée automatiquement via useMemo | Tout jeu de flashcards avec un grand dataset et un subset aléatoire par partie — le `useMemo` évite les bugs de Map périmée après replay |

## Références

- [BDR-047](../decisions/BDR-047.md) — décision buildRoundPool 1 photo par personnage
